import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

export const gamingLaptopsSlice = createSlice({
    name: 'gamingLaptops',
    initialState,
    reducers: {
        
    }
})

export const {  } = gamingLaptopsSlice.actions;
export default gamingLaptopsSlice.reducer;