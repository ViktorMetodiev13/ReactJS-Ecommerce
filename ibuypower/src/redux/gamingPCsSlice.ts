import { createSlice } from "@reduxjs/toolkit";

const initialState = {

}

export const gamingPCsSlice = createSlice({
    name: 'gamingPCs',
    initialState,
    reducers: {
        
    }
})

export const {  } = gamingPCsSlice.actions;
export default gamingPCsSlice.reducer;